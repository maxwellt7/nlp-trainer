import { PlaceholderFeature } from '../src/ui/PlaceholderFeature';

export default function AudiosScreen() {
  return (
    <PlaceholderFeature
      title="Audios"
      note="Audio script library, render polling, and in-app playback migration is queued in the next implementation slice."
    />
  );
}
