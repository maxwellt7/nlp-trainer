import { PlaceholderFeature } from '../src/ui/PlaceholderFeature';

export default function ReferenceScreen() {
  return (
    <PlaceholderFeature
      title="Reference"
      note="NLP codex search and categorization will be ported with offline-friendly caching after core session parity."
    />
  );
}
