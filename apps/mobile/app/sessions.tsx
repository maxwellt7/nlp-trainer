import { PlaceholderFeature } from '../src/ui/PlaceholderFeature';

export default function SessionsScreen() {
  return (
    <PlaceholderFeature
      title="Sessions"
      note="Session history and expanded detail views will be ported with list virtualization and pull-to-refresh."
    />
  );
}
