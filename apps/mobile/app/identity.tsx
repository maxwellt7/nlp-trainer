import { PlaceholderFeature } from '../src/ui/PlaceholderFeature';

export default function IdentityScreen() {
  return (
    <PlaceholderFeature
      title="Identity"
      note="Values hierarchy, conflict mapping, and evidence details will be migrated after session and audio flows."
    />
  );
}
